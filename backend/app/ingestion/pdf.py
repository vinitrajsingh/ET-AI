"""
STUB — PDF ingestion.

Future job: extract text (and page images where needed) from PDF manuals,
regulations, and incident reports using Docling + PyMuPDF, returning normalized
text blocks with page numbers for downstream entity extraction and chunking.
"""
