"""
STUB — Image ingestion (OCR).

Future job: run OCR on scanned forms and photos (Docling OCR) to recover text,
returning normalized text blocks. Non-diagram images only; P&ID drawings are
handled separately in pid.py.
"""
