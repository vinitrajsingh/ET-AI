"""
STUB — Email ingestion.

Future job: parse .eml archives with Python's stdlib `email` module — headers
(from/to/date/subject), body, and attachments — turning scattered mail threads
into normalized text blocks and routing attachments to the right ingester.
"""
