"""
STUB — Ingestion router.

Future job: expose upload endpoints that accept heterogeneous files, dispatch to
the right ingester in app.ingestion, run entity extraction, merge facts into the
Neo4j graph, and embed chunks into Qdrant. Will define its own APIRouter and be
included in app.main.
"""
