"""
STUB — Embeddings service.

Future job: wrap OpenAI `text-embedding-3-small` (1536-dim) to turn document
chunks and queries into vectors for Qdrant. Will expose batch embedding and a
single-query helper, using the model/dim from app.config.
"""
