# SANJEEVANI backend — pinned for Python 3.11
# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # multipart/form-data (file uploads)

# Agent orchestration + LLM
langgraph==0.2.60
langchain==0.3.14
langchain-openai==0.2.14
openai==1.59.6

# Databases
neo4j==5.27.0                     # Neo4j AuraDB driver (bolt+s://)
certifi==2024.12.14               # CA bundle used to verify Aura's TLS cert
qdrant-client==1.12.1             # Qdrant vector store client
sqlalchemy==2.0.36                # ORM for Neon Postgres
psycopg2-binary==2.9.10           # Postgres driver (SSL for Neon)

# Config / validation
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Document processing / ingestion
docling==2.14.0                   # PDF / OCR / P&ID parsing
pymupdf==1.25.1                   # fast PDF text/image extraction
python-docx==1.1.2                # .docx
openpyxl==3.1.5                   # .xlsx
pandas==2.2.3                     # tabular normalization

# Voice (Guru Mode). NOTE: openai-whisper's old setup.py needs pkg_resources,
# which modern setuptools (>=81) drops, and torch needs setuptools>=77. Install it
# after the rest with a compatible setuptools and no build isolation:
#   pip install "setuptools==79.0.1"
#   pip install --no-build-isolation openai-whisper==20240930
# The audio path also needs ffmpeg on PATH (winget install Gyan.FFmpeg). Without
# ffmpeg, Guru Mode still works via the typed-transcript fallback.
openai-whisper==20240930          # local Whisper transcription (needs ffmpeg)

# PDF (audit package)
reportlab==4.2.5                  # audit-package PDF rendering

# Tests
pytest==8.3.4
httpx==0.28.1                     # FastAPI TestClient dependency
